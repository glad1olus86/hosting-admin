version: "3.8"
services:
  hosting-admin:
    build: .
    container_name: hosting-admin
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - HESTIA_HOST=https://host.docker.internal:8083
      - HESTIA_USER=SuperAdmin
      - HESTIA_PASSWORD=Afonya_rulit1
      - NODE_TLS_REJECT_UNAUTHORIZED=0
      - SSH_HOST=host.docker.internal
      - SSH_PORT=49222
      - SSH_USER=serveradmin
      - SSH_KEY_PATH=/keys/id_ed25519
    volumes:
      - ./keys:/keys:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
